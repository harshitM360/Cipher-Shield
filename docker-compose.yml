version: "3.9"

services:
  ransomware-copilot-streamlit:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    command: >
      streamlit run streamlit_app.py
      --server.address=0.0.0.0
      --server.port=8501
